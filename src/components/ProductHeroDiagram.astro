---
interface Props {
  label: string;
  items: [string, string, string, string];
  variant?: 'orbit' | 'axis' | 'steps' | 'route' | 'collective' | 'focus';
}

const { label, items, variant = 'orbit' } = Astro.props;
---

<figure
  class:list={['product-diagram', `product-diagram--${variant}`]}
  role="img"
  aria-label={`${label} : ${items.join(', ')}`}
>
  <figcaption>{label}</figcaption>
  <ol aria-hidden="true">
    {items.map((item, index) => <li><span>{String(index + 1).padStart(2, '0')}</span>{item}</li>)}
  </ol>
</figure>

<style>
  .product-diagram {
    position: relative;
    min-height: 22rem;
    margin: 0;
    overflow: hidden;
    color: var(--color-brand-deep);
    background:
      linear-gradient(rgba(18, 54, 95, 0.07) 1px, transparent 1px),
      linear-gradient(90deg, rgba(18, 54, 95, 0.07) 1px, transparent 1px),
      rgba(255, 255, 255, 0.78);
    background-size: 3rem 3rem;
    border: 1px solid rgba(18, 54, 95, 0.14);
    border-radius: var(--radius-panel);
    box-shadow: var(--shadow-panel);
  }

  .product-diagram::before,
  .product-diagram::after {
    position: absolute;
    content: '';
    pointer-events: none;
  }

  figcaption {
    position: absolute;
    z-index: 2;
    top: 50%;
    left: 50%;
    max-width: 10rem;
    padding: var(--space-3) var(--space-4);
    color: var(--color-surface);
    background: var(--color-brand-deep);
    border-radius: var(--radius-pill);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-heading);
    letter-spacing: 0.06em;
    text-align: center;
    text-transform: uppercase;
    transform: translate(-50%, -50%);
  }

  ol {
    position: absolute;
    inset: 0;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  li {
    position: absolute;
    z-index: 1;
    min-height: 2.75rem;
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-4);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-pill);
    box-shadow: var(--shadow-card);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-strong);
  }

  li span {
    color: var(--color-gold-dark);
    font-size: var(--font-size-xs);
  }

  li:nth-child(1) { top: 10%; left: 8%; }
  li:nth-child(2) { top: 13%; right: 7%; }
  li:nth-child(3) { bottom: 11%; left: 10%; }
  li:nth-child(4) { right: 8%; bottom: 14%; }

  .product-diagram--orbit::before {
    inset: 16%;
    border: 1px solid rgba(201, 154, 70, 0.52);
    border-radius: 50%;
  }

  .product-diagram--orbit::after {
    inset: 29%;
    border: 1px solid rgba(18, 54, 95, 0.16);
    border-radius: 50%;
  }

  .product-diagram--axis::before {
    top: 12%;
    bottom: 12%;
    left: 50%;
    width: 1px;
    background: linear-gradient(var(--color-gold), var(--color-border));
  }

  .product-diagram--axis::after {
    top: 50%;
    right: 10%;
    left: 10%;
    height: 1px;
    background: linear-gradient(90deg, var(--color-border), var(--color-gold), var(--color-border));
  }

  .product-diagram--steps::before {
    top: 20%;
    right: 17%;
    bottom: 20%;
    left: 17%;
    border: 1px solid rgba(201, 154, 70, 0.44);
    transform: rotate(-8deg);
  }

  .product-diagram--steps::after {
    top: 30%;
    right: 9%;
    bottom: 30%;
    left: 9%;
    border: 1px solid rgba(18, 54, 95, 0.16);
    transform: rotate(8deg);
  }

  .product-diagram--route figcaption {
    top: 50%;
    left: 50%;
  }

  .product-diagram--route li:nth-child(1) { top: 9%; left: 6%; }
  .product-diagram--route li:nth-child(2) { top: 29%; right: 8%; }
  .product-diagram--route li:nth-child(3) { bottom: 27%; left: 9%; }
  .product-diagram--route li:nth-child(4) { right: 6%; bottom: 8%; }

  .product-diagram--route::before {
    top: 17%;
    right: 18%;
    bottom: 17%;
    left: 18%;
    border: 1px solid rgba(201, 154, 70, 0.5);
    border-width: 1px 1px 1px 0;
    transform: skewY(18deg);
  }

  .product-diagram--route::after {
    inset: 24% 28%;
    border: 1px solid rgba(18, 54, 95, 0.16);
    transform: rotate(36deg);
  }

  .product-diagram--collective li:nth-child(1) { top: 7%; left: 50%; transform: translateX(-50%); }
  .product-diagram--collective li:nth-child(2) { top: 46%; right: 5%; }
  .product-diagram--collective li:nth-child(3) { bottom: 7%; left: 50%; transform: translateX(-50%); }
  .product-diagram--collective li:nth-child(4) { top: 46%; right: auto; bottom: auto; left: 5%; }

  .product-diagram--collective::before {
    inset: 17% 25%;
    border: 1px solid rgba(201, 154, 70, 0.48);
    transform: rotate(45deg);
  }

  .product-diagram--collective::after {
    inset: 30%;
    border: 1px solid rgba(18, 54, 95, 0.17);
    border-radius: 50%;
  }

  .product-diagram--focus figcaption {
    top: 15%;
  }

  .product-diagram--focus li:nth-child(1) { top: 38%; left: 7%; }
  .product-diagram--focus li:nth-child(2) { top: 38%; right: 7%; }
  .product-diagram--focus li:nth-child(3) { right: 7%; bottom: 9%; left: auto; }
  .product-diagram--focus li:nth-child(4) { right: auto; bottom: 9%; left: 7%; }

  .product-diagram--focus::before {
    top: 22%;
    bottom: 18%;
    left: 50%;
    width: 1px;
    background: linear-gradient(var(--color-gold), var(--color-border));
  }

  .product-diagram--focus::after {
    top: 54%;
    right: 18%;
    left: 18%;
    height: 1px;
    background: linear-gradient(90deg, var(--color-border), var(--color-gold), var(--color-border));
  }

  @media (max-width: 640px) {
    .product-diagram {
      min-height: 19rem;
    }

    li {
      max-width: 9.5rem;
      font-size: var(--font-size-xs);
    }

    .product-diagram--route li,
    .product-diagram--focus li {
      max-width: 8.25rem;
      padding-inline: var(--space-3);
    }

    .product-diagram--focus li {
      padding-inline: var(--space-2);
    }

    .product-diagram--collective li:nth-child(2) { right: 2%; }
    .product-diagram--collective li:nth-child(4) { left: 2%; }
  }
</style>
